@model IEnumerable<HoTuanPhuoc.Models.SACH>

@{
    ViewBag.Title = "sachtheochude";
    Layout = "~/Views/Shared/LayoutUser.cshtml";
}

<h2>Sách theo chủ đề <strong> @ViewBag.tenChuDeSach  </strong></h2>

<hr>
<div class="row text-center">
    @foreach (var sach in Model)
    {
        <div class="col-sm-4 col-md-4 col-lg-4 col-xs-6">
            <div class="thumbnail book-thumbnail">
                <img src="~/Images/@sach.AnhBia" alt="Thumbnail Image" class="img-responsive img-rounded imgbook" />
                <div class="caption">
                    <h4 style="min-height:70px;">
                        <a href="@Url.Action("BookDetail", "HoTuanPhuoc", new { id = sach.MaSach })">@sach.TenSach</a>
                    </h4>
                    <p>
                        <a href="@Url.Action("ThemGioHang", "GioHang", new { ms = sach.MaSach, url = Request.Url.ToString() })" class="btn btn-primary" role="button">
                            <span class="glyphicon glyphicon-shopping-cart" aria-hidden="true"></span> Add to Cart
                        </a>
                    </p>
                </div>
            </div>
        </div>
    }

</div>

<style>
    .book-thumbnail {
        height: 500px; /* Set a fixed height for thumbnails */
        display: flex;
        flex-direction: column; /* Ensure items stack vertically */
        justify-content: space-between; /* Space out items evenly */
    }
</style>